# PhD 科研工作台工作规则

1. 每次在我的本地更新完 PhD 科研工作站，不直接删除待清理内容；将本轮产生的临时文件、测试输出、验收记录和旧版本残留统一移动到项目根目录的 `delete/`，由用户检查后自行一键删除。更新和整理过程不得删除、覆盖或移动任何用户数据。
2. “测试”指本轮生成的测试输出、截图和缓存；`tests/`、`scripts/` 中可重复使用的测试源码属于项目源码，不作为临时文件删除。
3. 完成更新并验证后，将 `test-results/`、`验收记录/`、临时构建目录、打包调试日志、旧版本安装包及其他已核实的版本残留移动到 `delete/<日期>-<版本>/`；保留原有名称或目录结构，避免覆盖已经存在的待删除内容。`release/` 只保留最新版安装包、对应的 SHA-256 校验文件及最新版可运行目录。
4. `工作台数据/`、SQLite 数据库、用户 PDF、笔记、任务、项目、投稿记录、壁纸、API 密钥、保存位置配置和数据库定位文件均为用户数据，任何更新或清理都不得删除、覆盖或重置。
5. 更新前后应以只读方式核对数据库完整性、关键表记录、逐项设置和被引用文件；测试必须使用隔离的 `WORKBENCH_DATA_DIR`，不得向正式数据写入样例。
6. 移动前必须逐项核实来源和目标的绝对路径均位于项目目录内，确认没有重解析点，并区分“可重新生成的程序产物”和“用户数据”；性质不明确的内容不得移入 `delete/`，应保留原处并向用户说明。
7. 每个批次在对应目录内写入 `待删除清单.md`，记录原路径、大小、归类理由和移动时间，便于用户检查；完成移动后仅验证原位置已空出、目标文件数量和总字节数一致，不替用户删除 `delete/`。
8. 上传 GitHub Release 时，发布说明只写版本概述以及功能新增、改进与修复；不得写入应用安装或覆盖升级指引、用户数据保护或保留说明、测试与构建验证结果、验收结论、SHA-256 或校验说明，也不得设置“安装与数据”“验证”“校验”等相关章节。安装包与 `SHA256SUMS.txt` 仍可作为 Release 资产上传，但不在发布说明正文中解释。
