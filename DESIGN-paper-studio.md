# 研究手记与成果藏书

## 范围

本次设计调整研究计划的信息层级、中央内容区、科研成果的展示与展开交互。保留任务标题输入、项目选择、AI 识别开关、搜索、排序、日历、任务详情及成果编辑/阅读功能。移除左侧项目分类与“全部项目”独立页面，但沿用现有任务、项目与论文数据，不删除项目记录、不增加数据迁移。

## 共同语言

- 纸张是内容容器：计划用轻薄便签，成果用有厚度的书本。背景和导航继续使用应用已有的半透明材质。
- 十二种低饱和鹅黄、粉、鼠尾草绿、雾蓝、淡紫、杏色等构成便签色系；同一总览中的前十二张便签不重复。书脊采用墨蓝、砖红、麦色、灰绿等较沉稳的颜色。颜色用于区分物件，不代表任务优先级或论文等级。
- 使用原系统字体。页面标题、物件标题、辅助信息三级层次；成果书页标题使用衬线字体，正文沿用系统字体。便签圆角 14px、间距 18px、内边距 18–22px，最小高度 236px；纸面与面板底色混合降低饱和度，不使用反光条、纸面纹理或伪元素光斑。
- 深色模式使用较深的彩色纸面与浅色文字；PDF 首页保持原白纸。长标题换行，信息卡可滚动，小窗口减少列数。

## 研究计划：按项目摆放便签

- 输入栏与工具栏位置保持。其下默认显示便签网格，宽窗口三列，空间不足自动变为两列或一列。
- 每张便签对应一个现有项目。卡头显示项目名与当前视图的“已完成 / 总数”；卡内逐行展示任务、圆形勾选框、安排时间、截止日期及优先级。完成任务保留划线状态，可以恢复。
- Today Plan 仅显示日期归属为今天的任务（包括当天已完成项），全部任务保留所有日期；项目页只显示当前项目。无归属的旧任务单独显示，归档项目只在有匹配任务时出现。
- 没有筛选时保留空项目便签，提供明确的空状态与右下角加号。加号选中该项目并聚焦原输入栏，不新建一套输入流程。快速新建栏始终提供“新建项目”，创建后直接选中新项目；便签右上角提供项目设置。
- 任务详情和便签内手动排序沿用已有交互。Today Plan 提供“便签 / 时间安排”切换，原分钟时间轴仍可使用；全部任务承担原项目总览职责。

## 科研成果：私人学术书架

- 每篇已有成果成为一本书，书脊展示标题、作者和年份。厚度与高度按稳定的论文标识做轻微变化，仅是视觉装帧，不暗示页数、影响力或质量。
- 书架按可用宽度自动换层，每层有细窄搁板和柔和投影。顶部保留总数、搜索、排序；左侧年份及期刊筛选继续有效。
- 悬停时书本向上抽出少许，键盘聚焦只显示焦点框。点击后先抽离书架，再围绕书缝翻开右侧封面；总动作约 830ms，避免回弹。关闭时合上封面并返回书架，恢复焦点且书脊回正；不让焦点动画覆盖归位姿态。系统“减少动态效果”开启时改为直接淡入淡出。
- 展开为左右等宽暖白纸页，中央使用局部书缝阴影，底边呈现纸张厚度。左页呈现真实 PDF 第一页并预留页脚，右页依次为论文标题、作者、期刊/会议、发表时间、期刊分区、JCR 分区、DOI、论文荣誉与备注。缺失字段明确标注“暂未填写”，不推测。
- 卡片提供“阅读论文”“编辑发表信息”和关闭入口，支持 Escape、遮罩关闭、键盘焦点约束。完整标题和元数据可选择复制；PDF 不可用时显示占位说明，信息仍可查看编辑。
- 书架下方同步提供紧凑成果列表，列出标题、作者、发表时间、期刊/会议与分区，不显示选择框；标题与操作按钮可直接阅读或编辑同一成果，不复制数据。

## 验证约定

使用隔离 WORKBENCH_DATA_DIR 核验任务添加/完成/恢复/编辑/排序、日期筛选、原时间轴、成果搜索/筛选/编辑/阅读、键盘关闭、深浅色、最小窗口、大字号与减少动态效果。正式数据库、设置和引用 PDF 仅做更新前后的只读比对。本轮产物按 AGENTS.md 归入 delete/ 待用户检查。
